{
  "name": "mdserve",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "bun run build && vite preview",
    "deploy": "bun run build && wrangler deploy --strict",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@ox-content/wasm": "2.16.0",
    "hono": "4.12.23"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.38.0",
    "@types/node": "25.9.1",
    "vite": "8.0.14",
    "vite-ssr-components": "0.6.1"
  }
}
